# Local PNG style-edit template

Use only after a candidate passes every image-level blocking rule and only within the one-edit budget.

```text
Edit this candidate locally. Preserve every major region, object count, group membership, stage order, fan-out/fan-in, reading direction, and scientific relationship exactly.

Change only: {{one_style_issue}}
Allowed edit class: palette | saturation | one glyph appearance | one decoration | local whitespace | unwanted ornament

Do not add, delete, move, merge, split, relabel, or reconnect scientific objects. Do not change indices, equations, centroids, source/target, arrow direction, connector routing, or backward multiplicity. If the requested change requires one of those operations, stop and revise the blueprint/skeleton instead.
```
