# Synthetic restoration note

This example is intentionally fictional and contains no experimental data or unpublished method.

A synthetic measurement `y` is passed to one abstract restoration model `f_θ`. The model produces one estimate `x̂`. The figure must show a single left-to-right path and must not imply measured performance, clinical validity, training data, or comparison with another method.
