# Historical reconstruction: Candidate C hybrid

**Review outcome:** `REJECTED_FOR_REGION_WORKFLOW_AND_EXPORT_DEFECTS`.

This compact capsule preserves the visible result of an early mixed-media Candidate C reconstruction. It improved visual fidelity, but formula-aspect and region-workflow defects prevented approval. The exact selection remains in [`../candidate_selection_c_only.json`](../candidate_selection_c_only.json), with the full sequence in [`../selection_history.json`](../selection_history.json).

![Rejected Candidate C hybrid reconstruction](preview.png)

The full historical package is preserved in the immutable [`v0.1.0` tag](https://github.com/XinyuanWang283/sketch-to-scientific-figure/tree/v0.1.0/examples/deep_image_prior/editable_delivery_c_hybrid). It is intentionally omitted from current `main`; this preview is not the active result or evidence of scientific approval. Use the canonical [`editable_delivery_c_fidelity_v2/`](../editable_delivery_c_fidelity_v2/README.md) package for the verified reference case.
